server:
  port: 8068
  servlet:
    context-path: /employeedashboard
 

logging:
  file:  C:\logs\employeedashboard.log

feign:
  hystrix:
    enabled: true 
    
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream